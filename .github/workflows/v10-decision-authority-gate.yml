name: V10 Decision Authority Gate

on:
  pull_request:
    paths:
      - "src/lib/v10DecisionAuthorityGate.ts"
      - "src/lib/__tests__/v10DecisionAuthorityGate.test.ts"
      - "src/lib/__tests__/v10DecisionAuthorityGate.conformance.test.ts"
      - "testdata/v10-decision-cycle-conformance.json"
      - ".github/workflows/v10-decision-authority-gate.yml"
  push:
    branches: [main]
    paths:
      - "src/lib/v10DecisionAuthorityGate.ts"
      - "src/lib/__tests__/v10DecisionAuthorityGate.test.ts"
      - "src/lib/__tests__/v10DecisionAuthorityGate.conformance.test.ts"
      - "testdata/v10-decision-cycle-conformance.json"
      - ".github/workflows/v10-decision-authority-gate.yml"
  workflow_dispatch:

permissions:
  contents: read

env:
  EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  verify:
    name: Verify independent V10 decision gate
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    steps:
      - name: Check out exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.EXPECTED_HEAD_SHA }}
          persist-credentials: false

      - name: Verify exact head
        shell: bash
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD_SHA"

      - name: Set up Node 24
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Typecheck
        run: npm run typecheck

      - name: Verify decision authority gate and cross-repo identity
        run: npx vitest run src/lib/__tests__/v10DecisionAuthorityGate.test.ts src/lib/__tests__/v10DecisionAuthorityGate.conformance.test.ts
