name: GitHub Governance Preflight

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/github-governance-preflight.yml'
      - '.github/workflows/reconcile-fcr-governance-phase.yml'
      - 'scripts/audit-github-governance-preflight.mjs'
      - 'scripts/audit-github-governance-preflight.node-test.mjs'
      - 'scripts/audit-fcr-governance-phase.mjs'
      - 'scripts/audit-fcr-governance-phase.node-test.mjs'
      - 'src/providers/fcrGovernancePhase.ts'
      - 'src/providers/FounderControlRoomGovernanceGitHubProvider.ts'
  push:
    branches: [main]
    paths:
      - '.github/workflows/github-governance-preflight.yml'
      - '.github/workflows/reconcile-fcr-governance-phase.yml'
      - 'scripts/audit-github-governance-preflight.mjs'
      - 'scripts/audit-github-governance-preflight.node-test.mjs'
      - 'scripts/audit-fcr-governance-phase.mjs'
      - 'scripts/audit-fcr-governance-phase.node-test.mjs'
      - 'src/providers/fcrGovernancePhase.ts'
      - 'src/providers/FounderControlRoomGovernanceGitHubProvider.ts'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  governance-contract:
    name: GitHub Governance Preflight Contract
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out exact candidate
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - name: Set up Node 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Verify legacy independent-review preflight contract
        run: node --test scripts/audit-github-governance-preflight.node-test.mjs
      - name: Verify phase-aware governance contract
        run: node --test scripts/audit-fcr-governance-phase.node-test.mjs

  governance-provider-read:
    name: GitHub Governance Preflight Provider Read
    if: >-
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
    needs: governance-contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    steps:
      - name: Check out trusted main commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Set up Node 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Read live GitHub governance state for current phase
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_APP_ID: ${{ secrets.APP_ID }}
          FCR_GOVERNANCE_TARGET_REF: main
          FCR_GOVERNANCE_PHASE: founder_only
        run: node scripts/audit-fcr-governance-phase.mjs
      - name: Publish sanitized governance receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: github-governance-phase-preflight-${{ github.run_id }}
          path: artifacts/github-governance-phase-preflight.json
          if-no-files-found: error
          retention-days: 14
