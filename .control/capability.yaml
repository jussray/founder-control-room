# Legacy compatibility pointer only.
# Canonical capability authority is .control/capability.json.
# This file intentionally carries no independent capability, health, deploy,
# rollback, proof, or verification state. Consumers must read the JSON contract.
kind: fcr/capability-legacy-pointer@v1
authority: non-authoritative
canonical_source: .control/capability.json
