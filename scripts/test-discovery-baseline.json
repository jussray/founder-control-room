{
  "note": "All .test.ts and .test.js files under src/ are discovered by the default Vitest include pattern 'src/**/*.test.{ts,js}'. The base-bound ratchet checks every supported test/spec suffix and currently records no excluded debt.",
  "includePattern": "src/**/*.test.{ts,js}",
  "undiscovered": []
}
