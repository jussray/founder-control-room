export const callbackJs = `const title=document.getElementById('callback-title'),message=document.getElementById('callback-message'),back=document.getElementById('callback-return');function fail(text){title.textContent='Login could not be completed';message.textContent=text;back.hidden=false;history.replaceState(null,'','/auth/callback')}const query=new URLSearchParams(location.search),fragment=new URLSearchParams(location.hash.slice(1)),description=query.get('error_description')||fragment.get('error_description');if(description){fail(description)}else{const access=fragment.get('access_token'),refresh=fragment.get('refresh_token');if(!access||!refresh){fail('The login response is missing session credentials or has expired. Start again.')}else{try{const response=await fetch('/auth/session',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({access_token:access,refresh_token:refresh})});const data=await response.json();history.replaceState(null,'','/auth/callback');if(!response.ok)throw new Error(data.error||'Session verification failed');location.replace('/')}catch(error){fail(error instanceof Error?error.message:'Session verification failed')}}}`;
