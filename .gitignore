node_modules/
dist/
dist-pages/

# Local environment / secret files. Commit only explicit safe templates.
.env
.env.*
!.env.example
!.env.*.example
.dev.vars
.dev.vars.*
!.dev.vars.example
!.dev.vars.*.example

*.log
.DS_Store
.wrangler/
e2e/.auth-bridge.json
__pycache__/
*.pyc
playwright-report/
test-results/

# Generated locally by scripts/verify-documentation-truth.mjs; CI uploads it
# as a workflow artifact instead of committing it. Scoped to this exact file
# so other artifacts/ content (decision records, provenance) stays tracked.
artifacts/documentation-truth-report.json

# HubSpot CLI local authentication and account context
hubspot.config.yml
.hsaccount
.hs/
