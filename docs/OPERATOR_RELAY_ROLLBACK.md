# Operator Relay Rollback

The relay slice is additive. If validation or runtime integration fails, remove or revert the operator-relay files without changing existing DeepSeek instructor interop, shared ULTRATHINK runtime, repository providers, or production authority paths.

Do not weaken existing auth or provider boundaries to make the relay pass.
