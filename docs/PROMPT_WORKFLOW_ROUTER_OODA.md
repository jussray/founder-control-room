# OODA — Prompt Workflow Router

OBSERVE: contract, selector, registry, route module, tests and proof gates exist; real server mount does not.

ORIENT: the missing mount is the single cause preventing runtime reachability. Adding more prompt definitions would not advance the launch claim.

DECIDE: change only `src/http/server.ts` with one import and one mount, behind the existing browser mutation gate.

ACT: after the mount, run source gate → focused Vitest → PR CI → exact-SHA deployment → Playwright runtime proof.

STOP: do not widen scope until the real path is green.
