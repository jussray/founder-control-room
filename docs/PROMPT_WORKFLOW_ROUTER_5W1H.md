# 5W1H — Prompt Workflow Router

Who: the authenticated founder/trusted FCR controller selects workflow intent; external content does not.

What: map a known builder/decision intent to a small deterministic reasoning/evidence stack.

Where: FCR source of truth, with PromptOS/Chief/provider adapters consuming the same contract rather than inventing competing authority.

When: selection occurs before analysis/execution; proof occurs again after any consequential implementation.

Why: retain the usability of one-line prompts while preserving evidence, falsification, rollback, and authority separation.

How: pure selector → founder-gated HTTP adapter → existing authority plane → task-specific execution/proof path.
