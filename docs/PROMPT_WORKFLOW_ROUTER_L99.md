# L99 — Prompt Workflow Router

Authority: founder-gated selection only; no execution grant.
State identity: branch + exact commit SHA must bind proof.
Evidence: source readback, focused tests, CI, deployed `/version`, Playwright runtime proof.
Rollback: additive files plus two-line server mount; no migration.
Blast radius: prompt workflow selection surface only.
Recovery: unmount route or revert focused patch.
Compounding value: one deterministic routing vocabulary can be shared by FCR, PromptOS, Chief adapters, and provider-neutral instruction surfaces without duplicating authority logic.
