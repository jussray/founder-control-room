# Binding packet

This packet intentionally stops at canonical identity, read/discovery provenance, non-authority invariants, and verification requirements. External UI implementation remains a separate runtime task.
