# Chief Integration Boundary

Chief may consume the selected workflow stack for reasoning/capability composition. FCR remains the authority/control plane.

Chief must not infer approval from:
- the selected mode stack;
- model capability;
- confidence;
- continuity fingerprints;
- prompt text;
- provider availability.

Any consequential execution continues through the existing proposal/approval/receipt path.
