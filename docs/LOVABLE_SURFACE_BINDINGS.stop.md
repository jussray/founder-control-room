# Stop condition

Repository-side work is sufficient when the exact candidate passes its required checks and the public-safe binding is deployed. Do not add more FCR architecture to compensate for an external builder that cannot yet accept its own source/UI integration changes.
