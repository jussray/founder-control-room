{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Preserve current workflow, Cloudflare, Shopify, founder-merge, content-metrics provenance, public-discovery, and cross-repository StoryEngine federation truth boundaries without converting source, analytics, CSV imports, crawler policy, peer pinning, provider configuration, mergeability, or candidate/browser proof into publication, production, provider-mutation, merge, deploy, spend, or external-contact authority.",
  "domains": [
    "cloudflare-authority",
    "repository-provider",
    "content-evidence",
    "evidence-authority",
    "publishing",
    "merge-authority",
    "workflow-authority"
  ],
  "changes": [
    {
      "path": "public/_worker.js",
      "claims": [
        "`public/_worker.js` must route public static discovery artifacts, including `crawlers.json`, through the Pages `ASSETS` binding while API-owned callback assets remain on `FCR_API`; this routing classification cannot authorize provider mutation or treat a static crawler policy as authentication."
      ]
    },
    {
      "path": ".github/workflows/playwright.yml",
      "claims": [
        "`.github/workflows/playwright.yml` must bind the FCR-to-StoryEngine browser federation witness to the separately verified exact StoryEngine main peer SHA observed for the candidate and must re-read that peer ref before execution; changing either repository head or peer identity expires predecessor federation/browser proof and requires the complete exact-head witness to rerun without granting merge, deploy, production, or provider-mutation authority."
      ]
    },
    {
      "path": ".github/workflows/ci.yml",
      "claims": [
        "`.github/workflows/ci.yml` must keep the existing crawler and public-work Playwright specs inside the load-bearing `Playwright e2e` job that feeds `Required Gate`; a separate advisory Pages workflow cannot substitute for required exact-head browser execution or grant merge authority."
      ]
    },
    {
      "path": "wrangler.worker.toml",
      "claims": [
        "`wrangler.worker.toml` must list `TINYFISH_API_KEY` under `[secrets].required`, so canonical exact-main Deploy rejects missing provider-held TinyFish activation before Worker mutation while never copying the secret value through GitHub Actions."
      ]
    },
    {
      "path": ".github/workflows/fcr-access-front-door-recovery.yml",
      "claims": [
        "`.github/workflows/fcr-access-front-door-recovery.yml` must keep FCR product authentication inside the app, may detach only browser-facing public destinations from one uniquely identified mixed Cloudflare Access application, must preserve every non-browser destination and existing policy, and must fail closed on ambiguous or public-only provider state."
      ]
    },
    {
      "path": "src/http/routes/approvals.ts",
      "claims": [
        "`src/http/routes/approvals.ts` must require exact-head evidence plus provider-backed PR identity, deterministic independent review, and authenticated founder-final confirmation bound to the exact PR, base, and head before FCR integration, then must re-read the mutable branch head immediately before provider integration."
      ]
    },
    {
      "path": "README.md",
      "claims": [
        "`README.md` must keep TinyFish source and CI readiness separate from provider/runtime activation and state that canonical Deploy verifies only provider-held binding-name presence before promotion.",
        "`README.md` must describe Shopify child-provider observation as source-only, non-authorizing evidence and must never relabel a matching snapshot as live provider authority or mutation permission.",
        "`README.md` must document Attack 3000 content currentness as independently evaluated from bounded observation time rather than trusted from a CURRENT label, and must keep distribution, external demand, publication, provider mutation, merge, deploy, spend, and external-contact authority separate.",
        "`README.md` must describe the Content Trend Radar as an advisory existing-lane capability whose first-wave evidence requires current source provenance and whose ranking cannot authorize publication, scheduling, spend, provider mutation, merge, deploy, or external contact.",
        "`README.md` must describe deterministic founder-content approval reservation and exact active retry recovery without allowing evidence rotation, retry, or a stored reservation to mint new publication authority.",
        "`README.md` must describe `content-metrics-csv@v1` as an observation-only import contract that binds account/page identity, audience segment, metric name and unit, measurement windows, null semantics, duplicate handling, and provenance while keeping real provider readback separate from fixture proof.",
        "`README.md` must describe the normalized founder-content analytics interchange as requiring separate account/page identity and offset-aware timestamps, and must keep filename/display-name/receipt-generation provenance separate from stable logical idempotency and provider truth."
      ]
    },
    {
      "path": "docs/CLOUDFLARE_REASONING.md",
      "claims": [
        "`docs/CLOUDFLARE_REASONING.md` must treat `TINYFISH_API_KEY` as a provider-held Worker binding and require canonical Deploy to fail closed on a missing binding name without exposing or re-uploading its value."
      ]
    },
    {
      "path": "docs/TRUTH_DECAY_AUDIT.md",
      "claims": [
        "`docs/TRUTH_DECAY_AUDIT.md` must treat a StoryEngine peer SHA/ref in FCR Playwright as evidence identity rather than a durable alias for current StoryEngine, and must require complete federation re-proof whenever the peer identity or either repository head moves.",
        "`docs/TRUTH_DECAY_AUDIT.md` must keep imported content metrics observation-only, preserve UNKNOWN for missing values, reject conflicting duplicate identities, and require real provider evidence before imported fixture data can support a current external analytics claim.",
        "`docs/TRUTH_DECAY_AUDIT.md` must distinguish stable logical analytics-import identity from provenance-only filename, display-name, and receipt-generation metadata so presentation changes cannot manufacture a second logical import."
      ]
    },
    {
      "path": "docs/deployment/CLOUDFLARE_WORKER_TARGETS.md",
      "claims": [
        "`docs/deployment/CLOUDFLARE_WORKER_TARGETS.md` must name `TINYFISH_API_KEY` as a required canonical Worker secret while preserving live provider observation and real TinyFish Search/Fetch as separate activation proof."
      ]
    },
    {
      "path": "docs/SECRETS.md",
      "claims": [
        "`docs/SECRETS.md` must keep the TinyFish key in the Cloudflare Worker secret plane and never recast the provider-held value as a GitHub Actions secret or proof artifact."
      ]
    },
    {
      "path": "src/lib/providerObservation.ts",
      "claims": [
        "`src/lib/providerObservation.ts` must classify child-provider evidence as current, undeclared, scope drift, identity drift, stale, or unknown without granting execution authority, and it must fail closed when observation freshness, completeness, declaration identity, or scope equality is not proven."
      ]
    },
    {
      "path": "src/providers/ShopifyReadOnlyProvider.ts",
      "claims": [
        "`src/providers/ShopifyReadOnlyProvider.ts` must bind Shopify observation to the exact Founder Control Room permanent and branded store identities, require complete pagination and app scope inventory, withhold CURRENT on incomplete or mismatched provider evidence, keep reconciliation non-authorizing, and never persist provider inventory or perform Shopify mutation."
      ]
    },
    {
      "path": "src/lib/contentMetricsCsv.ts",
      "claims": [
        "`src/lib/contentMetricsCsv.ts` must parse only the bounded `content-metrics-csv@v1` schema, bind each observation to provider/account/page/audience/content/window/source provenance, preserve blank metric values as UNKNOWN_NO_EVIDENCE, collapse only identical duplicate identities, reject conflicting duplicates, and remain observation-only without freshness, publication, strategy-mutation, or provider authority."
      ]
    },
    {
      "path": "tools/founder-content-contracts/content-analytics-csv-ingest.cjs",
      "claims": [
        "`tools/founder-content-contracts/content-analytics-csv-ingest.cjs` must require explicit account and page identity, offset-aware receipt/snapshot timestamps, bounded normalized CSV input, null-preserving metrics, duplicate rejection within snapshots, deterministic overlap reconciliation through the canonical analytics audit, and observation-only authority.",
        "Its logical `idempotency_key` must bind canonical normalized snapshot evidence plus platform/account/page/comparison scope and normalized top-post scope; exact source SHA-256, filename, account display label, and receipt-generation time remain provenance only and must not change logical identity when normalized evidence is unchanged. Reordering otherwise identical CSV rows must not mint a second import, while changed metric evidence must change the key."
      ]
    },
    {
      "path": "src/ultrathink-core/attack3000Content.ts",
      "claims": [
        "`src/ultrathink-core/attack3000Content.ts` must bind publication and metrics to one content observation, independently evaluate evaluatedAt, window ordering, future skew, observedAt, explicit bounded expiry, measurement completion, and evidence, and must reject stale or future observations even when a caller labels freshness CURRENT.",
        "`src/ultrathink-core/attack3000Content.ts` must preserve metric-local stop floors and direct external-demand evidence without treating publication, reach, reactions, comments, profile views, or generic analytics references as business demand or execution authority."
      ]
    },
    {
      "path": "src/ultrathink-core/__tests__/attack3000Content.test.ts",
      "claims": [
        "`src/ultrathink-core/__tests__/attack3000Content.test.ts` must prove stale-at-evaluation, future-dated, overlong-lease, mixed-identity, incomplete-window, demand-evidence, metric-local floor, profile-view, and non-authorizing behavior instead of trusting caller freshness labels."
      ]
    },
    {
      "path": "src/ultrathink-core/contentTrendRadar.ts",
      "claims": [
        "`src/ultrathink-core/contentTrendRadar.ts` must require at least one evidence id with source and observedAt that is current against an explicit evaluation time before first-wave eligibility, while keeping prediction-only, stale, future-dated, malformed, unattributed, and incompletely framed candidates out of the first wave.",
        "`src/ultrathink-core/contentTrendRadar.ts` must keep ranking advisory and must never authorize publication, scheduling, spend, provider mutation, merge, deploy, or external contact."
      ]
    },
    {
      "path": "src/ultrathink-core/__tests__/contentTrendRadar.test.ts",
      "claims": [
        "`src/ultrathink-core/__tests__/contentTrendRadar.test.ts` must adversarially cover missing provenance, stale and future evidence, prediction-only signals, required content angles, saturation, duplicate fingerprints, deterministic first-wave selection, and the non-authorizing ceiling."
      ]
    },
    {
      "path": "src/ultrathink-core/index.ts",
      "claims": [
        "`src/ultrathink-core/index.ts` may export the Attack 3000 content and Trend Radar evidence contracts, including the evaluation context, only as source capabilities and must not imply provider activation, publication, merge, deploy, spend, or execution authority."
      ]
    },
    {
      "path": "src/lib/founderContentApprovalStore.ts",
      "claims": [
        "`src/lib/founderContentApprovalStore.ts` must reserve the same public thesis/opening-hook pattern within one founder, platform, and Current You intent under one deterministic approval id, and a retry may recover only the exact matching active stored proposal/public-payload reservation while rejecting mismatched, consumed, revoked, expired, or non-current state.",
        "`src/lib/founderContentApprovalStore.ts` must return the original stored authorization hash and timestamps on exact retry recovery and must never use a retry, deterministic identity, or evidence rotation to create or renew publication authority."
      ]
    },
    {
      "path": "src/lib/__tests__/founderContentApprovalStore.test.ts",
      "claims": [
        "`src/lib/__tests__/founderContentApprovalStore.test.ts` must prove exact retry recovery after a lost issuance response while preserving the existing one-reservation behavior across source SHA, evidence reference, evidence digest, and proposal-hash rotation."
      ]
    },
    {
      "path": "docs/founder-signal-engine/linkedin-analytics-continuity.md",
      "claims": [
        "`docs/founder-signal-engine/linkedin-analytics-continuity.md` must document independently evaluated content freshness, direct demand evidence, current trend provenance, fingerprint comparison, deterministic approval retry identity, and observation-only authority without rewriting historical evidence when currentness expires."
      ]
    }
  ]
}
