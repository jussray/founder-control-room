{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Bind create-branch authority lifetime to the server-observed execute request time while preserving isolated Founder OS determinism, fresh pre-provider mission-state revalidation, and the existing no-merge/no-deploy authority ceiling.",
  "domains": [
    "merge-authority"
  ],
  "changes": [
    {
      "path": "src/http/routes/approvals.ts",
      "claims": [
        "`src/http/routes/approvals.ts` must capture the authenticated execute request time at the HTTP boundary and inject it into create-branch authority issuance; execution must still re-read current mission state and use fresh execution time immediately before provider mutation."
      ]
    }
  ]
}
