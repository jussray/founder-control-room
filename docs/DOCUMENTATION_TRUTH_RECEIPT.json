{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Document only the current Founder Control Room and Chief AI standalone-peer carrier: exact candidate peer bindings, non-transferring authority, a load-bearing pair status check that is available on every pull request, and Documentation Truth coverage for the pair-enforcement surface. This receipt does not claim runtime activation, deployment, publication, provider mutation, merge approval, or shared-runtime dependence.",
  "domains": [
    "repository-provider",
    "pair-authority",
    "publishing",
    "truth-governance"
  ],
  "changes": [
    {
      "path": "src/providers/providerFactory.ts",
      "claims": [
        "`src/providers/providerFactory.ts` must require the exact `verify-pair-contract` context alongside Required Gate and the exact-head test-ledger context for Founder Control Room main governance, while the workflow producer must remain available to every pull request so the required check cannot deadlock unrelated work."
      ]
    },
    {
      "path": "config/founder-chief-pair.contract.json",
      "claims": [
        "`config/founder-chief-pair.contract.json` must keep Founder Control Room and Chief AI independently callable standalone peers, prohibit identity, receipt, failure, continuity, or implicit authority collapse, and require fresh exact approval for the active publication path where the stricter executable contract demands it."
      ]
    },
    {
      "path": ".control-room/founder-chief-pair-peer-lock.json",
      "claims": [
        "`.control-room/founder-chief-pair-peer-lock.json` must bind FCR PR #781 to the exact observed Chief pair, necessary-fix policy, and founder-content-strategy carrier head SHAs while remaining non-authorizing evidence that cannot grant merge, deployment, provider mutation, publication, or runtime truth."
      ]
    },
    {
      "path": "scripts/verify-founder-chief-pair.mjs",
      "claims": [
        "`scripts/verify-founder-chief-pair.mjs` must fail closed when standalone-peer identity, lifecycle, receipt, failure, continuity, publication-approval, or necessary-fix authority invariants drift, and it cannot relabel static repository alignment as deployed runtime truth."
      ]
    },
    {
      "path": ".github/workflows/founder-chief-pair-contract.yml",
      "claims": [
        "`.github/workflows/founder-chief-pair-contract.yml` must produce the required `verify-pair-contract` context on every pull request; when the pair-enforcement surface changes it must validate both exact live Chief pair-authority heads against the immutable FCR peer lock, while unrelated FCR changes must not become dependent on Chief candidate state."
      ]
    },
    {
      "path": "scripts/verify-documentation-truth.mjs",
      "claims": [
        "`scripts/verify-documentation-truth.mjs` must classify the shared pair contract, FCR candidate peer lock, pair verifier, and pair workflow as pair-authority truth-sensitive surfaces and fail closed when the required current documentation or path-bound receipt is missing."
      ]
    }
  ]
}
