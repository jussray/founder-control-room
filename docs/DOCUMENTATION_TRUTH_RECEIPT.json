{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Bind the PromptOS founder-permission execution membrane, exact-head provider effect, and Documentation Truth registration repair to current path invariants without claiming that any live PromptOS promotion has occurred.",
  "domains": [
    "founder-permission-authority",
    "repository-provider",
    "truth-governance"
  ],
  "changes": [
    {
      "path": "src/http/routes/founderPermissions.ts",
      "claims": [
        "src/http/routes/founderPermissions.ts must keep PromptOS client-visible receipts non-authorizing, verify trusted provider preconditions before one-shot consumption, execute the registered PromptOS effect only server-side after exact atomic consumption, require provider-native outcome verification, and forbid blind retry after a consumed ambiguous or failed effect."
      ]
    },
    {
      "path": "src/lib/founderPermissionBroker.ts",
      "claims": [
        "src/lib/founderPermissionBroker.ts must bind promptos_workflow_registry_promote to the canonical repository and main branch, exact head, workflow and registry hashes, provider identity, capability version, and consequence class while recorded founder decisions remain executionAuthorized false."
      ]
    },
    {
      "path": "src/lib/founderPermissionExecution.ts",
      "claims": [
        "src/lib/founderPermissionExecution.ts must derive mutable execution state from the trusted repository provider, verify workflow body identity plus exact workflow and registry hashes, build only the approved registry entry, revalidate immediately before effect, and require exact post-write branch and registry readback before outcomeVerified true."
      ]
    },
    {
      "path": "src/providers/PromptOSRegistryWriter.ts",
      "claims": [
        "src/providers/PromptOSRegistryWriter.ts must restrict mutation to jussray/promptos main workflows/registry.json, require the founder-approved pre-head as the new commit parent, use a non-forced ref update, reconcile ambiguous responses only through authoritative provider readback, and never expose ambient repository write authority."
      ]
    },
    {
      "path": "scripts/verify-documentation-truth.mjs",
      "claims": [
        "scripts/verify-documentation-truth.mjs must classify the founder-permission route, broker, and execution production paths as founder-permission-authority and require docs/PORTABLE_FOUNDER_APPROVALS.md so future authority changes cannot false-green documentation coverage."
      ]
    }
  ]
}
