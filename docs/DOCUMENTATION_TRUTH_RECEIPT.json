{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Bind the current Founder Control Room to Chief AI standalone-peer enforcement, pair-status merge gate, repository-provider governance, and documentation-truth classifier to the exact current PR scope without converting source, peer locks, evidence, green checks, or documentation into merge, deploy, provider-mutation, publication, runtime, or production authority.",
  "domains": [
    "pair-authority",
    "repository-provider",
    "truth-governance"
  ],
  "changes": [
    {
      "path": ".control-room/founder-chief-pair-peer-lock.json",
      "claims": [
        "`.control-room/founder-chief-pair-peer-lock.json` must bind FCR PR #781 to the exact observed Chief pair and necessary-fix candidate heads while remaining non-authorizing evidence that cannot grant merge, deployment, provider mutation, publication, or runtime truth."
      ]
    },
    {
      "path": ".github/workflows/founder-chief-pair-contract.yml",
      "claims": [
        "`.github/workflows/founder-chief-pair-contract.yml` must rerun full cross-repository pair validation whenever a load-bearing pair-verifier input changes, must fail closed on Chief peer-head drift, and must reserve the cheap no-op success path only for pull requests whose pair-enforcement dependency surface is genuinely unchanged."
      ]
    },
    {
      "path": "scripts/verify-founder-chief-pair.mjs",
      "claims": [
        "`scripts/verify-founder-chief-pair.mjs` must remain the load-bearing local and cross-repository standalone-peer verifier and must reject identity, lifecycle, receipt, failure, continuity, publication-authority, or necessary-fix policy drift without allowing evidence to create cross-peer authority."
      ]
    },
    {
      "path": "scripts/verify-documentation-truth.mjs",
      "claims": [
        "`scripts/verify-documentation-truth.mjs` must classify the shared pair contract, candidate peer lock, pair verifier, and pair workflow as pair-authority truth-sensitive surfaces and must require current path-bound documentation evidence instead of allowing those authority changes to inherit stale documentation green."
      ]
    },
    {
      "path": "src/providers/providerFactory.ts",
      "claims": [
        "`src/providers/providerFactory.ts` must require the exact `verify-pair-contract` status context alongside `Required Gate` and `Verify test-ledger contract` for active Founder Control Room main governance, so standalone-peer enforcement cannot remain advisory at merge time."
      ]
    }
  ]
}
