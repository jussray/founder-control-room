{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Record the repository-provider boundary for the read-only PR truth audit without claiming merge, deployment, provider mutation, credential installation, or live production authority.",
  "domains": [
    "repository-provider"
  ],
  "changes": [
    {
      "path": "src/providers/providerFactory.ts",
      "claims": [
        "src/providers/providerFactory.ts must construct the PR audit through the canonical lazy repository-provider path, preserve GitHub App credential preference and project mapping, and fail closed when the configured provider lacks bounded PR audit evidence."
      ]
    },
    {
      "path": "src/providers/RepositoryProvider.ts",
      "claims": [
        "src/providers/RepositoryProvider.ts must expose PR audit evidence only as an optional provider-neutral read capability; consumers cannot infer merge, approval, mutation, or credential authority from this interface."
      ]
    },
    {
      "path": "src/providers/PullRequestAuditEvidence.ts",
      "claims": [
        "src/providers/PullRequestAuditEvidence.ts must keep initial and final PR observations separate and requires bounded collections to carry explicit completeness metadata so truncation cannot be treated as complete evidence."
      ]
    },
    {
      "path": "src/providers/GitHubAuditRepositoryProvider.ts",
      "claims": [
        "src/providers/GitHubAuditRepositoryProvider.ts must keep GitHub-specific API access inside the repository-provider family, bound returned observations to at most 100 per collection, preserve newest review evidence when capped, and never add GitHub mutation authority."
      ]
    },
    {
      "path": "src/providers/GitHubPrTruthReader.ts",
      "claims": [
        "src/providers/GitHubPrTruthReader.ts must remain a provider-backed MCP adapter only; it cannot import the GitHub SDK, mint or return provider credentials, or bypass the canonical RepositoryProvider project boundary."
      ]
    }
  ]
}
