{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Bind the explicit founder_only and independent_review governance-phase repair to current repository-provider documentation while preserving exact-head, CodeQL, pull-request, force-push, deletion, trusted-App, readback, and non-authorizing evidence boundaries.",
  "domains": [
    "repository-provider"
  ],
  "changes": [
    {
      "path": "src/providers/fcrGovernancePhase.ts",
      "claims": [
        "src/providers/fcrGovernancePhase.ts must require explicit founder_only before zero outside approving reviews can be accepted, while historical one-or-more-review configurations remain independent_review-compatible and cannot silently infer the weaker human-review phase."
      ]
    },
    {
      "path": "src/providers/FounderControlRoomGovernanceGitHubProvider.ts",
      "claims": [
        "src/providers/FounderControlRoomGovernanceGitHubProvider.ts must encode founder_only and independent_review without changing the shared pull-request, CodeQL, force-push, deletion, trusted-App pull-request-only bypass, review-thread, and zero-bypass strict exact-head freshness floor, and provider readback must reject phase drift."
      ]
    },
    {
      "path": "src/providers/providerFactory.ts",
      "claims": [
        "src/providers/providerFactory.ts must route canonical Founder Control Room GitHub App governance through the phase-aware provider, preserve repository-identity binding and exact trusted-App bypass validation, and reject a zero-review FCR main policy unless founder_only is explicitly selected."
      ]
    }
  ]
}
