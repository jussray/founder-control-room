{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Bind the current founder-content editorial-novelty and publication-memory, provider-neutral n8n authority, cadence-expiry reservation, observation-only analytics supersession, ULTRATHINK V4 advisory anti-laundering, and current GitHub App credential-transport changes to reviewed source invariants without granting merge, deploy, publication, provider-policy, database, secret, billing, destructive-action, or runtime authority.",
  "domains": [
    "publishing"
  ],
  "changes": [
    {
      "path": "src/lib/founderEditorialNovelty.ts",
      "claims": [
        "src/lib/founderEditorialNovelty.ts must keep PromptOS, Chief, and FCR roles separate; compare LinkedIn candidates against a bounded union of published/analyzed linkedin_experiments, successful first-party publication receipts, and founder-attested LinkedIn observations; reject exact published payload/copy identities or HIGH semantic repetition; and never upgrade attested evidence into provider verification or publication authority.",
        "src/lib/founderEditorialNovelty.ts must keep publication memory hash-only: prior public copy may be represented by bounded hashes and metadata, but raw prior public copy must never be retained merely to enforce novelty."
      ]
    },
    {
      "path": "src/lib/__tests__/founderEditorialNovelty.publicationMemory.test.ts",
      "claims": [
        "src/lib/__tests__/founderEditorialNovelty.publicationMemory.test.ts must prove that successful publication receipts and founder-attested copy hashes enter non-authorizing novelty history, exact published copy is blocked without semantic text, all three history planes are unioned in bounded recency order, and raw public copy is absent from the history projection."
      ]
    },
    {
      "path": "src/lib/founderContentApprovalStore.ts",
      "claims": [
        "src/lib/founderContentApprovalStore.ts must run the LinkedIn editorial novelty gate after canonical proposal validation but before persisting one-shot founder-content approval; HIGH repetition risk must fail before approval-store mutation, while novelty fingerprints remain continuity evidence and never become approval or publication authority."
      ]
    },
    {
      "path": "src/http/routes/n8nConveyor.ts",
      "claims": [
        "src/http/routes/n8nConveyor.ts must require authenticated founder execution through the FCR-owned authoritative n8n approval adapter, reject caller-supplied approval objects, and must not treat route acceptance as publication truth."
      ]
    },
    {
      "path": "src/lib/n8nFounderContentAuthorityAdapter.ts",
      "claims": [
        "src/lib/n8nFounderContentAuthorityAdapter.ts must preflight n8n transport, provider allowlisting, provider/platform compatibility, cadence and approval-expiry bounds, source-project resolution, and durable execution reservation before consuming one-shot authority; only after those non-provider gates succeed may it atomically claim the exact FCR-stored approval and permit provider dispatch, while a failed claim must stop before provider write and leave an auditable retryable pre-provider reservation state."
      ]
    },
    {
      "path": "src/lib/founderContentCadence.ts",
      "claims": [
        "src/lib/founderContentCadence.ts must require the exact founder approval expiry as part of cadence reservation input, pass that expiry into the authoritative database RPC, reject requests whose initial schedule is already outside the approval lease before touching durable cadence authority, and fail closed if database readback ever returns a reserved slot at or beyond approval expiry."
      ]
    },
    {
      "path": "src/lib/n8nFounderContentOrchestrator.ts",
      "claims": [
        "src/lib/n8nFounderContentOrchestrator.ts must carry the exact already-validated Current You approval expires_at into every canonical founder-content cadence reservation before any external n8n orchestration, so the canonical dispatcher cannot reserve or dispatch a schedule outside the founder approval lease."
      ]
    },
    {
      "path": "src/lib/n8nProviderNeutralFounderContentPreparation.ts",
      "claims": [
        "src/lib/n8nProviderNeutralFounderContentPreparation.ts must complete provider-neutral envelope construction, transport/provider readiness, cadence reservation atomically bounded by the exact approval expiry, source-project resolution, and durable approval_executions reservation before one-shot founder approval is consumed; post-reservation expiry validation remains defense in depth, a claim failure may re-arm only an execution explicitly recorded as retryable_before_provider with provider_write_attempted=false, and preparation itself must never send the provider request."
      ]
    },
    {
      "path": "supabase/migrations/20260830113000_founder_content_cadence_expiry_guard.sql",
      "claims": [
        "supabase/migrations/20260830113000_founder_content_cadence_expiry_guard.sql must preserve the serialized rolling-hour cadence lane while replacing the four-argument reservation RPC with an expiry-bound service-role-only contract that raises before INSERT whenever the computed or existing slot is at or beyond the supplied founder approval expiry; the migration must not delete prior cadence evidence."
      ]
    },
    {
      "path": "src/lib/n8nProviderNeutralFounderContentOrchestrator.ts",
      "claims": [
        "src/lib/n8nProviderNeutralFounderContentOrchestrator.ts must derive deferred-provider schedule envelopes from canonical exact-current-you proposal and approval authority, preserve provider/platform compatibility and the review-only boundary, and must never treat n8n or provider acceptance as final publication truth."
      ]
    },
    {
      "path": "docs/PUBLIC_COMMUNICATION_TRUTH_CONTRACT.md",
      "claims": [
        "docs/PUBLIC_COMMUNICATION_TRUTH_CONTRACT.md must require the same FCR-owned one-shot approval membrane for provider-neutral n8n writes, must preflight configuration before consumption, and must keep provider-native readback as terminal publication evidence."
      ]
    },
    {
      "path": "README.md",
      "claims": [
        "README.md must describe the source-level n8n approval adapter without claiming deployed n8n, Buffer credentials, provider mutation, readback, or live publication outcome, while preserving current-main GitHub App credential transport truth."
      ]
    },
    {
      "path": "scripts/founder_content_supersession.py",
      "claims": [
        "scripts/founder_content_supersession.py must remain observation-only, require strictly newer same-subject cumulative observations with SHA-256 source digests, reject decreasing cumulative impressions or engagements, preserve the prior claim as SUPERSEDED_HISTORICAL, classify surprise, and bind the bounded strategy mutation into a deterministic receipt without granting approval or publication authority.",
        "scripts/founder_content_supersession.py must report claim_source_binding as NOT_LOCKED_V3 because carrying source digests cannot by itself prove that a claim was derived from those exact bytes, and predecessor receipt lineage must not be regenerated under a different canonicalization contract."
      ]
    },
    {
      "path": "scripts/test_founder_content_supersession.py",
      "claims": [
        "scripts/test_founder_content_supersession.py must prove the weaker-signal supersession transition, deterministic receipt identity including strategy mutation, strict timestamp ordering, and fail-closed rejection of decreasing cumulative metrics using synthetic source digests rather than private analytics provenance."
      ]
    },
    {
      "path": "docs/founder-signal-engine/linkedin-analytics-continuity.md",
      "claims": [
        "docs/founder-signal-engine/linkedin-analytics-continuity.md must keep aggregate daily LinkedIn ENGAGEMENT activity separate from per-post cumulative performance, document the observation-only ACTUAL-FLOW V3 supersession seam, and state that V3 source-digest presence is not V4 claim-to-source provenance locking."
      ]
    },
    {
      "path": "packages/ultrathink-verification-core/src/v4-advisory-handoff.v0.ts",
      "claims": [
        "packages/ultrathink-verification-core/src/v4-advisory-handoff.v0.ts must keep the FCR-to-Chief V4 advisory boundary evidence-only and capped at ATTESTED, derive advisory identity locally from a canonical validated projection rather than trusting caller-selected receipt_sha256 as the integrity root, require exact source-byte binding and provenance semantics, keep unproven metrics and claims outside the handoff, and reject receipt/source/evidence inconsistencies without granting publication or execution authority."
      ]
    },
    {
      "path": "packages/ultrathink-verification-core/test/v4-advisory-handoff.v0.test.ts",
      "claims": [
        "packages/ultrathink-verification-core/test/v4-advisory-handoff.v0.test.ts must preserve the sanitized five-field ATTESTED handoff contract and Attack Ten regressions for authority escalation, canonicalization drift, verification laundering, source-verification downgrade, false derivation claims, source-binding spoofing, digest disagreement, receipt identity mismatch, evidence injection, and replayed-digest freezing."
      ]
    },
    {
      "path": "src/providers/githubAppAuth.ts",
      "claims": [
        "src/providers/githubAppAuth.ts may normalize only transport-equivalent forms of one complete GitHub App private RSA PEM: raw PEM, escaped-newline or CRLF PEM, JSON-quoted PEM, and base64-encoded PEM; the normalized key must parse through Node createPrivateKey as a private RSA key for RS256, malformed/truncated/non-RSA material must fail closed with a bounded non-secret configuration error, and normalization must not be treated as proof of App identity, installation scope, token minting, provider acceptance, or mutation authority."
      ]
    }
  ]
}