{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Bind the current evidence-authority Playwright federation, privileged production-witness changes, retained FCR repository-provider governance invariants, and the Chief governance observation/reconciliation boundary to truthful documentation without claiming live GitHub, Chief ruleset mutation, StoryEngine, Cloudflare, deployment, merge, publication, database, secret, billing, or destructive-action proof.",
  "domains": [
    "repository-provider",
    "evidence-authority"
  ],
  "changes": [
    {
      "path": "src/providers/GitHubProvider.ts",
      "claims": [
        "src/providers/GitHubProvider.ts must derive Code Owner and last-push human-review requirements from whether the canonical native approving-review count is greater than zero, while preserving pull-request enforcement, review-thread resolution, CodeQL, trusted-App bypass limits, force-push/deletion protection, strict-freshness separation, and provider readback."
      ]
    },
    {
      "path": "src/providers/providerFactory.ts",
      "claims": [
        "src/providers/providerFactory.ts must keep the current FCR founder-only native approving-review floor at zero while retaining the constitutional required checks and trusted GitHub App boundary, and a later deliberate positive native-review count must remain representable without weakening the existing machine or founder-final authority membrane."
      ]
    },
    {
      "path": "src/providers/githubGovernanceReconciliation.ts",
      "claims": [
        "src/providers/githubGovernanceReconciliation.ts must keep Chief governance reconciliation pinned to jussray/chief-ai-machine and ruleset IDs 20818149 and 21261587, preserve required-check integration_id producer identity and unrelated checks, require complete bypass-state evidence, and refuse to describe a read-then-PUT update of existing rulesets as concurrency-safe when no atomic provider precondition is available.",
        "src/providers/githubGovernanceReconciliation.ts may return NO_CHANGE_REQUIRED only for already-correct observed provider state; a required live change remains BLOCKED_PROVIDER_ATOMIC_PRECONDITION_UNAVAILABLE and neither disposition grants provider mutation, merge, deploy, or execution authority."
      ]
    },
    {
      "path": "src/providers/trustedGithubGovernanceObserver.ts",
      "claims": [
        "src/providers/trustedGithubGovernanceObserver.ts must observe only the fixed Chief repository and fixed governance rulesets through FCR's repository-scoped GitHub App installation-token path, reject caller-supplied PAT/token authority, require complete provider readback suitable for fingerprinting, and remain evidence-only rather than minting ruleset-mutation, merge, deploy, or execution authority."
      ]
    },
    {
      "path": ".github/workflows/playwright.yml",
      "claims": [
        ".github/workflows/playwright.yml must keep the StoryEngine product-build federation witness bound to the canonical root directive wire shape, require both node-test and playwright proof, and verify the exact peer runtime identity before and after execution without granting merge, deploy, production-promotion, or provider-mutation authority.",
        ".github/workflows/playwright.yml must keep the privileged production workflow_run witness restricted to successful Deploy workflow_dispatch runs from main, execute only trusted witness source instead of the Deploy run head, and treat workflow_run.head_sha as release evidence rather than executable authority."
      ]
    }
  ]
}
