{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Record the bounded invariants disclosed when truth-governance controls change; this is review evidence, not a live provider or production claim.",
  "domains": [
    "repository-provider",
    "truth-governance",
    "cloudflare-authority",
    "workflow-authority",
    "merge-authority"
  ],
  "changes": [
    {
      "path": ".github/workflows/cloudflare-build-diagnostic.yml",
      "claims": [
        ".github/workflows/cloudflare-build-diagnostic.yml: the exact-current-main Worker Git authority audit must require the dedicated read-only Workers Builds user token for its primary provider readback, while request-trace and DNS-inventory credentials remain optional enrichment and cannot suppress the core authority receipt; the workflow remains read-only and cannot authorize provider mutation."
      ]
    },
    {
      "path": "src/buildEvents/buildEvent.ts",
      "claims": [
        "src/buildEvents/buildEvent.ts: coverage events must retain bounded aggregate-only shape validation and cannot carry raw request evidence."
      ]
    },
    {
      "path": "src/buildEvents/currentTruth.ts",
      "claims": [
        "src/buildEvents/currentTruth.ts: webhook main facts must remain last-observed provenance, current coverage must use a fresh read-through GitHub revalidation and passed verified lease, and legacy external operational assertions cannot render as current."
      ]
    },
    {
      "path": "src/buildEvents/currentMainRevalidation.ts",
      "claims": [
        "src/buildEvents/currentMainRevalidation.ts: current-main identity must be read from the enrolled GitHub provider at display time, bind the enrolled repository and `main` branch, and reject a non-exact SHA."
      ]
    },
    {
      "path": "src/buildEvents/releaseCoverageWitness.ts",
      "claims": [
        "src/buildEvents/releaseCoverageWitness.ts: a passed coverage claim must require a server-owned exact-target Cloudflare witness, deployment-to-window ordering, an independently written aggregate digest, and the canonical App-aware repository credential path."
      ]
    },
    {
      "path": "src/http/routes/buildEventReceipts.ts",
      "claims": [
        "src/http/routes/buildEventReceipts.ts: the release-observer credential must be project-bound Cloudflare analytics coverage only, reject control-plane fields and non-passed receipts, and require the matching independent witness."
      ]
    },
    {
      "path": "scripts/verify-documentation-truth.mjs",
      "claims": [
        "scripts/verify-documentation-truth.mjs: truth-sensitive changes must reject punctuation-only or hidden-comment documentation edits and require a meaningful path-bound invariant receipt."
      ]
    },
    {
      "path": "src/providers/RepositoryProviderFactory.ts",
      "claims": [
        "src/providers/RepositoryProviderFactory.ts: repository reads that need production identity proof must reject partial GitHub App credentials and otherwise prefer a repository-scoped installation credential over an explicit local fallback."
      ]
    },
    {
      "path": "src/services/buildEventStore.ts",
      "claims": [
        "src/services/buildEventStore.ts: the bounded feed must retain the newest GitHub-observed main source as historical provenance without treating webhook arrival order as current-main proof."
      ]
    },
    {
      "path": "src/http/routes/buildEvents.ts",
      "claims": [
        "src/http/routes/buildEvents.ts: the current-truth route must bind projection evaluation to the enrolled project repository and fail closed when that repository identity is unavailable."
      ]
    },
    {
      "path": "src/governance/portfolioDecisionContext.ts",
      "claims": [
        "src/governance/portfolioDecisionContext.ts: evidence-dependent consequential portfolio execution must bind founder authorization to the exact normalized decision-context Truth Lease and re-evaluate fresh dependency observations at the use boundary; stale, changed, missing, invalid, or unbound truth must reconfirm instead of execute."
      ]
    },
    {
      "path": "src/governance/productionTruthLease.ts",
      "claims": [
        "src/governance/productionTruthLease.ts: a production-truth lease may exist only when Cloudflare Worker/Pages identity, Playwright tested/runtime identity, independent review, Supabase production state, and repository main identity are bound to one exact candidate; any changed, stale, missing, or mismatched dependency must fail closed at use time."
      ]
    },
    {
      "path": "wrangler.worker.toml",
      "claims": [
        "wrangler.worker.toml: every canonical Worker build must execute the repository build-authority membrane; native Cloudflare Workers Builds may proceed only through non-promoting versions upload while production promotion remains bounded to the exact manual GitHub authority path."
      ]
    },
    {
      "path": ".github/workflows/quality-gate.yml",
      "claims": [
        ".github/workflows/quality-gate.yml: every pull-request Quality Gate job must checkout and verify the exact `github.event.pull_request.head.sha`; a successful synthetic merge-ref run is merge-simulation evidence and cannot satisfy exact-head candidate proof."
      ]
    },
    {
      "path": "src/providers/GitHubProvider.ts",
      "claims": [
        "src/providers/GitHubProvider.ts: canonical FCR integration must retain provider-backed reviewed PR context, revalidate exact base/head authority, merge through GitHub's pull-request endpoint without a generic protected-branch fallback, apply/read back the zero-bypass strict-freshness ruleset before the review membrane, narrow the trusted App bypass to `pull_request`, receipt partial provider mutation identities, and preserve GitHub Check Run App issuer identity without fabricating it."
      ]
    },
    {
      "path": "src/providers/RepositoryProvider.ts",
      "claims": [
        "src/providers/RepositoryProvider.ts: verification signals may carry provider-backed GitHub App issuer identity so deterministic review can distinguish the trusted evidence producer from same-name green checks produced by another or unknown issuer."
      ]
    },
    {
      "path": "src/review/independentReviewGate.ts",
      "claims": [
        "src/review/independentReviewGate.ts: canonical founder-final deterministic review requires a numeric server-owned `GITHUB_APP_ID` and accepts the expected exact-head green GitHub witness only when its provider-backed App issuer matches that trusted identity; missing runtime identity, missing issuer, or mismatched issuer fails closed, while live GitHub ruleset enforcement remains separate provider truth."
      ]
    },
    {
      "path": "README.md",
      "claims": [
        "README.md: deterministic review authenticates the exact-head Check Run producer against server-owned `GITHUB_APP_ID`, while explicitly preserving live GitHub ruleset enforcement as a separate provider fact requiring provider readback."
      ]
    }
  ]
}
