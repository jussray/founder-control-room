{
  "contract": "fcr/documentation-truth-receipt@v1",
  "purpose": "Bind the read-only GitHub PR truth reader and universal remote MCP surface to current repository-provider documentation without claiming live OAuth activation, live GitHub App permission state, merge authority, deployment authority, provider mutation, or runtime support for an unproven client.",
  "domains": [
    "repository-provider"
  ],
  "changes": [
    {
      "path": "src/providers/GitHubPrTruthReader.ts",
      "claims": [
        "`src/providers/GitHubPrTruthReader.ts` must remain fixed to `jussray/founder-control-room`, mint only repository-scoped read permissions, omit patch bodies, bound provider collection, and fail closed when exact head, live base, required checks, reviews, or provider availability cannot be proven."
      ]
    },
    {
      "path": "README.md",
      "claims": [
        "`README.md` must describe the canonical FCR remote MCP as one provider-neutral read/preview capability surface and must keep MCP source compatibility separate from a live client connection, provider activation, or execution authority."
      ]
    },
    {
      "path": "docs/PROVIDERS.md",
      "claims": [
        "`docs/PROVIDERS.md` must document `github_audit_pr` as bounded repository-provider observation and must never treat its proof cookie, fingerprints, OAuth client identity, or GitHub App read token as merge, deploy, publication, or provider-mutation authority."
      ]
    }
  ]
}
