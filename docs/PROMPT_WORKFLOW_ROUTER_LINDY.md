# Lindy — Prompt Workflow Router

The implementation deliberately uses ordinary TypeScript data, a pure deterministic selector, Express routing, Vitest, GitHub Actions, and Playwright. It does not add a new agent framework, event bus, database table, or provider-specific orchestration layer.

The durable source of authority remains FCR's existing founder control decision contract. The prompt router only composes reasoning/evidence discipline around that authority.
