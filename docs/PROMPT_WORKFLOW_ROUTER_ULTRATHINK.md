# ULTRATHINK — Prompt Workflow Router

Consequence: reversible source/runtime routing change.
Authority: approved focused repository implementation; no widened merge/publication/execution authority.
Budget: repair.
Evidence: exact repo source and branch readback.
Serious hypotheses considered:
1. docs-only prompt catalog is sufficient;
2. PromptOS CRUD should own routing;
3. a pure deterministic selector plus founder-gated adapter should compose with existing authority.

Selected: #3. It adds executable routing behavior without coupling prompt templates to authority or creating a second orchestration system.

Stop condition: real server mount + focused proof + exact deployed Playwright proof. Do not expand into unrelated PromptOS or agent refactors.
