# One-way binding

The current binding is a read/discovery path from an external surface toward canonical repository identity. It does not authorize repository writes from the surface. Any future write path must use the repository's existing branch, pull-request, exact-head proof, review, and founder-final authority gates.
