# No-duplicate rule

The external surface manifest is discovery metadata only. It does not replace `src/config/portfolio.ts`, Founder OS project adapters, repository manifests, SITES contracts, or the FCR database. Those remain authoritative for their existing domains.

If a future implementation starts using this manifest to grant project, provider, merge, deploy, publication, or execution authority, that is a contract violation.
