# Provenance

Surface identity evidence and repository evidence are distinct. `surfaceCommit` identifies the observed Lovable source snapshot only. `canonicalRepository` and `canonicalBranch` identify where current project truth must be resolved. Neither value can substitute for the other.
