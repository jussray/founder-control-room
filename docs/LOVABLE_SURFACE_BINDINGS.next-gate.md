# Next gate

After this exact candidate passes the repository-required checks and the public manifest is deployed, the next gate is a single external-runtime task:

**Update Truth Compass to read the canonical FCR binding and durable FCR-backed data path, then prove signed-in create-claim → attach-evidence → reconcile → receipt + stale-cookie behavior with real browser coverage.**

Do not expand authority, add a second truth database, or treat Exact Match Engine seed/localStorage state as production while completing that gate.
