# Stop Condition

Do not add more prompt modes to this branch merely because more can be imagined.

The remaining product blocker is not prompt coverage. It is real runtime reachability and proof.

Next work must therefore be limited to:
1. `src/http/server.ts` mount;
2. focused verification;
3. PR/CI evidence;
4. exact-SHA deployment;
5. Playwright runtime proof.

Only evidence from those gates should reopen architecture scope.
