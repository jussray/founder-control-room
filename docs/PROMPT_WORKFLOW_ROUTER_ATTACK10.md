# Attack10 — Prompt Workflow Router

1. External prompt injection activates a mode. Control: inert external text boundary.
2. Mode selection grants execution. Control: hard false authority fields.
3. Unsupported intent guesses a stack. Control: fail closed.
4. Route exists but is never mounted. Control: server mount gate.
5. Unit test green but deployed code stale. Control: `/version` exact SHA.
6. Authenticated founder selection is mistaken for approval. Control: selection/decision contracts remain separate.
7. Provider outage silently substitutes another model. Control: canonical capability router forbids silent substitution.
8. Every task gets every mode and becomes slow/noisy. Control: smallest intent-specific stacks.
9. Adversarial prompts manufacture criticism. Control: evidence/falsification requirements.
10. Rollback requires migration. Control: additive, migration-free implementation.

Current surviving demonstrated failure: #4 until `src/http/server.ts` is mounted.
