# Paired MCP Attack Ten

Last reviewed: 2026-09-14

This matrix red-teams the external Chief AI + Founder Control Room MCP boundary. `SOURCE VERIFIED` means a repository test or static contract passes at the reviewed commit. It does not mean the provider configuration or deployed runtime has been proven.

| # | Attack | Required defense | Current proof |
| --- | --- | --- | --- |
| 1 | Unauthenticated discovery or tool call | Bearer challenge points to the protected-resource metadata URL; no tool dispatch | SOURCE VERIFIED: missing bearer tests return `401` with `WWW-Authenticate` |
| 2 | Forged, stale, premature, revoked, wrong-audience, or wrong-client token | Validate issuer, audience, client allowlist, `exp`, `nbf`, scope, project claim, current Supabase user, subject match, and founder allowlist | SOURCE VERIFIED: direct verifier tests cover issuer, audience, client, expiry, revocation, scope, and project grants. PROVIDER BLOCKED until OAuth is configured and exercised live |
| 3 | OAuth redirect or client-registration substitution | Canonical HTTPS resource metadata, exact issuer, exact client IDs, PKCE/provider controls | SOURCE VERIFIED for canonical metadata and client allowlist. PROVIDER BLOCKED for registered redirect URIs, PKCE, CIMD/DCR, and consent evidence |
| 4 | Protocol downgrade, request smuggling, or header/body confusion | Bounded JSON body, content type, supported-version allowlist, modern `_meta`, non-null ID, and matching protocol/method/tool headers | SOURCE VERIFIED by modern discovery, mismatch, null-ID, secret-input, and 64 KiB tests; legacy versions expose the same seven-tool authority ceiling |
| 5 | Cross-project confused deputy | Intersect token `mcp_projects` with server-held project scope before dispatch; recheck the exact project inside each tool | SOURCE VERIFIED: cross-project call is denied before the data dependency runs; `github_audit_pr` is additionally fixed to `founder-control-room` + `jussray/founder-control-room` |
| 6 | Mutation smuggled through a read/preview tool | No generic external nested invocation; fixed Chief ProofMode route; GitHub PR audit mints only a repository-scoped read-only installation token; skill actions limited to inspect/plan/review/draft; execution always false | SOURCE VERIFIED by seven-tool catalog, fixed-route, explicit GitHub App read-permission test, and rejected deploy-action tests |
| 7 | Prompt injection, patch leakage, or private skill exfiltration | Treat repository/project text as untrusted data; expose sanitized evidence and route metadata only; omit GitHub patch bodies; never return raw `SKILL.md` content | SOURCE VERIFIED by fixed response builders, GitHub audit patch-redaction test, and absence of a raw-skill tool. RUNTIME PROOF REQUIRED with adversarial stored rows |
| 8 | Credential, payload, cookie, or device-fingerprint leakage | Reject secret-bearing argument keys recursively; never accept caller credentials; hash evidence inputs/results; no MCP browser cookies or probabilistic/device fingerprints; semantic evidence fingerprints/cookies remain observation-only | SOURCE VERIFIED by nested-secret rejection, GitHub provider-error redaction, proof-cookie non-authority contracts, receipt privacy fields, and bounded payload tests |
| 9 | Success or provider failure without an audit trail | Persist a redacted `mcp_tool_calls` receipt after each returned read/preview result; convert provider read failures into sanitized fail-closed evidence where possible; fail the request closed if receipt persistence is unavailable | SOURCE VERIFIED by evidence-failure test and GitHub provider-denial result test. DATABASE/DEPLOYMENT PROOF still requires the exact live migration/runtime receipt |
| 10 | Stale-head, stale-base, replayed-plan, incomplete required-check set, or registry substitution | Bind GitHub audit to optional caller head, initial/final PR head, initial/final live base, provider-owned required-check discovery, exact-head check/status observations, bounded collection, and full head SHA; bind route preview to full head SHA, registry hash, project, provider, and capability-plan contract | SOURCE VERIFIED by GitHub live-base movement, expected-head, ambiguous-ruleset, bounded-read, and FCR routing contracts. DEPLOYMENT BLOCKED until same-SHA Worker, Supabase, GitHub App permission/readback, ChatGPT, and Claude evidence exists |

## Release rule

Do not call the paired MCP production-ready until every `BLOCKED` or `RUNTIME PROOF REQUIRED` cell has a timestamped, exact-head provider receipt. A source test cannot be promoted into evidence of a deployed OAuth flow, database schema, GitHub App permission grant, client connection, or runtime behavior.
