# /confess — Prompt Workflow Router

- NOT RUN: branch-local Vitest in this chat environment.
- NOT RUN: exact-head GitHub Actions, because the focused branch is not yet opened as a PR.
- BLOCKED: real `src/http/server.ts` mount is not yet present.
- NOT RUN: deployment of this branch.
- NOT RUN: deployed Playwright runtime proof.
- UNKNOWN: whether any unrelated repository checks will fail once a PR exists.

Do not translate any item above into `DONE`, `LIVE`, or `VERIFIED`.
